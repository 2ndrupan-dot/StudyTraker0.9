
[build]
  base = "artifacts/study-tracker"
  publish = "dist/public"
  command = "pnpm install && pnpm run build"

[build.environment]
  PORT = "5173"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
